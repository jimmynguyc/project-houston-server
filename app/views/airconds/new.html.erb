<%= form_for @aircond do |f|%>
	<%= f.fields_for :device do |device_field|%>
		<%= device_field.label :url%>
		<%= device_field.text_field :url %>
		<%= device_field.label :access_token%>		
		<%= device_field.text_field :access_token %>
	<%end%>
	<%= f.submit%>
<%end%>